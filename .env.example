# .env.example - Environment Configuration Template
# Copy this file to .env and configure for your environment

# Test Environment (AUTOMATION EXERCISE, APPLITOOLS, etc.)
# Corresponds to keys in src/main/resources/TestData.json
TEST_ENV=AUTOMATION EXERCISE

# Browser Type (chrome, firefox, edge)
BROWSER=chrome

# Headless Mode (true/false)
# Run browser without GUI (useful for CI/CD)
HEADLESS=false

# Screen Resolution (width,height)
SCREEN_RESOLUTION=1920,1080

# Hub Type (NONE, GRID)
# NONE = Local execution
# GRID = Selenium Grid execution
HUB_TYPE=NONE

